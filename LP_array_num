#include <stdio.h>

int main()
{
    int i;
    int numeros[5];
    
    for(i = 0; i < 5; i++){
        printf("Digite o %dº número: ", i+1);
        scanf("%d", &numeros[i]);
    }
    
    for(i = 0; i < 5; i++){
        printf("%d ", numeros[i]);
    }
    return 0;
}
